apply from: "https://raw.githubusercontent.com/Fuzss/modresources/main/gradle/${libs.versions.minecraft.get()}/forge.gradle"

dependencies {
    // Puzzles Lib
    modApi libs.puzzleslib.forge

    // Extensible Enums
    modApi(include(libs.extensibleenums.forge.get()))
}
